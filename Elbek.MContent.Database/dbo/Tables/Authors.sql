CREATE TABLE[dbo].[Authors] (
[Id] VARCHAR(36) NOT NULL PRIMARY KEY,
[Name] NVARCHAR (50) NOT NULL)
--TODO 3
--[Id] VARCHAR(36) Id у нас это Guid, какой тип данных в sql представляет собой Guid ?
-- тип Id переделать в uniqueidentifier
